apiVersion: v1
kind: Service
metadata:
  name: decrossword-service
  labels:
    app: decrossword
spec:
  selector:
    app: decrossword
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
